<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profile page</title>
  <link href="https://cdn.jsdelivr.net/npm/remixicon@3.7.0/fonts/remixicon.css" rel="stylesheet">
  <link rel="stylesheet" href="stylesheets/style_profile.css">
</head>
<body>
  <div class="w-full px-10 py-3 flex justify-between bg-zinc-800 font-semibold text-white">
    <h1 class="text-sky-400/100 text-3xl"><span class="text-white">Caption</span>Craze</h1>
    <div class="flex items-center gap-10">
      <a class=" hover:bg-zinc-700 text-white px-1 py-1 rounded inline-flex items-center" href="/profile">
        Profile
      </a>
       <a class=" hover:bg-zinc-700 text-white px-1 py-1 rounded inline-flex items-center" href="/feed">Feed</a> <a  href="/logout" class="px-3 py-2 bg-red-600 text-white rounded-lg inline-flex items-center hover:bg-red-700 ">Logout</a>
    </div>
  </div>
  <div class="profile w-full min-h-screen bg-zinc-800 text-white pt-1">
     <form hidden action="/fileupload" id="uploadform" method="post" enctype="multipart/form-data">
      <input id="forminput" type="file" name="image">
     </form>
    <div class="profdets flex flex-col items-center mt-10">
      <div class="relative">
        <span id="uploadicon" class="w-8 h-8 absolute bottom-0 right-0 rounded-full flex items-center justify-center bg-zinc-200 cursor-pointer"><i class="ri-pencil-fill text-zinc-800"></i></span>
        <div class="w-32 h-32 bg-zinc-200 rounded-full overflow-hidden" id="profile_pic">
          <img class="w-full h-full object-cover text-black" src="images/uploads/<%=user.profileImage%>" alt="">
        </div>
      </div>
      <h1 class="text-3xl font-sans  mt-3"><%=user.fullname%></h1>
      <h3 class="text-md">@<%=user.username%></h3>
      <a href="/edit" class="mt-3 px-7 py-2 bg-zinc-700 rounded-full text-xs font-semibold">Edit</a>
      <a href="/add" class="px-10 py-2 rounded-lg bg-blue-500 hover:bg-blue-700 text-xs font-semibold mt-2">Add new Post</a>
    </div>
    <div class="cards flex flex-wrap gap-10 px-10  mt-3">
      <div>
        <div class="card w-52 h-40 bg-zinc-200 rounded-lg overflow-hidden">
          <img class="w-full h-full object-cover hover:opacity-90" src="images/uploads/<%= user?.posts[0]?.image%>" alt="image">
        </div>
           <a href="/show/posts" class="text-l inline-block font-semibold mt-2 hover:opacity-90"> Your uploaded Posts</a>
        <h5 class="text-sm font-medium opacity-60"><%=user.posts.length%> Posts</h5>
      </div>
    </div>
  </div>


  <script src="https://cdn.tailwindcss.com"></script>
  <script src="javascripts/script_profile.js"></script>
</body>
</html>