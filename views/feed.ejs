<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All posts</title>
  <link href="https://cdn.jsdelivr.net/npm/remixicon@3.7.0/fonts/remixicon.css" rel="stylesheet">
  <link rel="stylesheet" href="stylesheets/style_upload.css">
</head>
<body>
  <div class="w-full px-10 py-3 flex justify-between bg-zinc-800 font-semibold text-white">
    <a href="/profile" class="text-sky-400/100 text-3xl"><span class="text-white">Caption</span>Craze</a>
    <div class="flex items-center gap-10">
        <a class=" hover:bg-zinc-700 text-white px-1 py-1 rounded inline-flex items-center" href="/profile">
            Profile
          </a>
           <a class=" hover:bg-zinc-700 text-white px-1 py-1 rounded inline-flex items-center" href="/feed">Feed</a> <a  href="/logout" class="px-3 py-2 bg-red-600 text-white rounded-lg inline-flex items-center hover:bg-red-700 ">Logout</a>
    </div>
  </div>
 <div class="w-full min-h-screen bg-zinc-800 px-10 py-8 text-white">
    <div class="cards flex flex-wrap gap-16">
           <%posts.forEach(function(elem){ %>

            <div class="w-32 card mt-3">
                <div class="w-40 h-40 bg-sky-200 rounded-lg overflow-hidden"  >
                   <img src="/images/uploads/<%=elem.image%>" alt="uploaded image" class="w-full h-full object-cover text-black" id="cardimg">
                </div>
                <h6 class="text-xs opacity-30 font-semibold hover:opacity-100 ">@<%= elem.user.username %></h6>
                <div class="w-40 mt-2 bg-gray-700 rounded-md shadow-md hover:shadow-lg transition duration-300">
                    <h5 class="mx-2 font-bold mb-1 hover:text-blue-100 transition duration-300 cursor-pointer"> <%= elem.title%></h5>
                    <p class="text-white w-full mx-2 hover:text-clip" style="word-wrap: break-word; white-space: pre-line;">
                      <%= elem.caption %>
                    </p>
                </div>
            </div>
          <% }) %>
    </div>
 </div>
  <script src="https://cdn.tailwindcss.com"></script>
</body>
</html>